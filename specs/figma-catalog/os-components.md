# OS Native Components Index

**File:** Native OS components
**File key:** `jUxTncguugL57ItYd6Iucy`
**Figma:** https://www.figma.com/design/jUxTncguugL57ItYd6Iucy/Native-OS-components

> System-level components managed by iOS and Android. These are references for understanding screen safe areas and system chrome — typically not part of design system components but appear in every screen mock.
> Each Node ID below points to the canonical component. To open it, build the URL from the file key + node ID (replace `:` with `-`), or pass the ID to `get_design_context(fileKey, nodeId)`.

---

## Components

| Component | Node ID |
|-----------|---------|
| MDS StatusBar | `1668:901` |
